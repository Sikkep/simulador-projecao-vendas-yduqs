{
  "name": "simulador-projecao-vendas-yduqs",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "description": "Simulador de projeção comercial para consultores e gerentes.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "audit": "node scripts/audit.mjs",
    "check": "npm run test && npm run audit && npm run build",
    "deploy": "vercel --prebuilt",
    "deploy:prod": "vercel --prebuilt --prod"
  }
}
